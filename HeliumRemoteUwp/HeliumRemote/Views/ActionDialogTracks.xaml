<ContentDialog
    x:Class="HeliumRemote.Views.ActionDialogTracks"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Button Content="Play now" Tag="0" Click="ButtonBase_OnClick" Grid.Row="0" HorizontalAlignment="Stretch" Margin="0,8,0,0" Style="{StaticResource ImButtonStyle}"/>
        <Button Content="Enqueue next" Tag="1" Click="ButtonBase_OnClick" Grid.Row="1" HorizontalAlignment="Stretch" Margin="0,8" Style="{StaticResource ImButtonStyle}"/>
        <Button Content="Enqueue last" Tag="2" Click="ButtonBase_OnClick" Grid.Row="2" HorizontalAlignment="Stretch" Style="{StaticResource ImButtonStyle}"/>
        <Button Content="Cancel" Margin="0,24,0,0" Tag="3" Click="ButtonBase_OnClick" Grid.Row="4" HorizontalAlignment="Stretch" Style="{StaticResource ImButtonStyle}"/>
    </Grid>
</ContentDialog>
